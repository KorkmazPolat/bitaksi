version: "3.9"

services:
  hr-assistant:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    environment:
      - CHROMA_PERSIST_DIR=/app/data/chroma
